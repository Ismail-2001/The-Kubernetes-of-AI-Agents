{{- if .Values.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-workflow-engine
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: workflow-engine
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/part-of: e-gaop
spec:
  replicas: {{ .Values.replicas | default 2 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: workflow-engine
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: workflow-engine
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: e-gaop
{{- include "e-gaop.prometheusAnnotations" . | nindent 6 }}
    spec:
{{- include "e-gaop.serviceAccountName" . | nindent 6 }}
      containers:
        - name: workflow-engine
{{- include "e-gaop.image" . | nindent 10 }}
          ports:
            - containerPort: {{ .Values.healthPort }}
              name: health
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-e-gaop-shared
            - configMapRef:
                name: {{ .Release.Name }}-workflow-engine
          env:
{{- include "e-gaop.commonEnv" (dict "serviceName" "workflow-engine" "healthPort" .Values.healthPort) | nindent 12 }}
{{- include "e-gaop.coreSecrets" . | nindent 12 }}
{{- include "e-gaop.probes" . | nindent 10 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
{{- include "e-gaop.containerSecurityContext" . | nindent 10 }}
{{- include "e-gaop.tlsVolumeMount" . | nindent 10 }}
{{- include "e-gaop.podSecurityContext" . | nindent 6 }}
{{- include "e-gaop.tlsVolume" (dict "Values" .Values "chartName" "workflow-engine") | nindent 6 }}
{{- end }}
