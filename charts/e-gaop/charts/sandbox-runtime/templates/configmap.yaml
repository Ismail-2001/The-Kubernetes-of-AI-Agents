{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-sandbox-runtime
  namespace: {{ .Release.Namespace }}
data:
  SANDBOX_RUNTIME_PORT: "{{ .Values.grpcPort }}"
  SANDBOX_RUNTIME_HEALTH_PORT: "{{ .Values.healthPort }}"
{{- end }}
