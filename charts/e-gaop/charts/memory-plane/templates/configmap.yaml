{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-memory-plane
  namespace: {{ .Release.Namespace }}
data:
  MEMORY_PLANE_PORT: "{{ .Values.grpcPort }}"
  MEMORY_PLANE_HEALTH_PORT: "{{ .Values.healthPort }}"
{{- end }}
