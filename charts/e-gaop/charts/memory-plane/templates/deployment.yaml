{{- if .Values.memory-plane.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-memory-plane
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: memory-plane
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/part-of: e-gaop
spec:
  replicas: {{ .Values.memory-plane.replicas | default 2 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: memory-plane
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: memory-plane
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: e-gaop
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.memory-plane.healthPort }}"
    spec:
      containers:
        - name: memory-plane
          image: "{{ .Values.memory-plane.image }}:{{ .Values.memory-plane.tag }}"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.memory-plane.grpcPort }}
              name: grpc
              protocol: TCP
            - containerPort: {{ .Values.memory-plane.healthPort }}
              name: health
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-e-gaop-shared
          env:
            - name: NODE_ENV
              value: production
            - name: OTEL_SERVICE_NAME
              value: egaop-memory-plane
            - name: SERVICE_GRPC_PORT
              value: "{{ .Values.memory-plane.grpcPort }}"
            - name: SERVICE_HEALTH_PORT
              value: "{{ .Values.memory-plane.healthPort }}"
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.memory-plane.resources | nindent 12 }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
      securityContext:
        fsGroup: 1001
{{- end }}
