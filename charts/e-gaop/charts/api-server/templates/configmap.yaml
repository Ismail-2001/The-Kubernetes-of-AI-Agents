{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-api-server
  namespace: {{ .Release.Namespace }}
data:
  API_SERVER_GRPC_PORT: "{{ .Values.grpcPort }}"
  API_SERVER_HEALTH_PORT: "{{ .Values.healthPort }}"
  API_SERVER_REST_PORT: "{{ .Values.restPort }}"
{{- end }}
